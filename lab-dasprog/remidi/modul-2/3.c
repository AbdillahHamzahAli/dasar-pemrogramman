#include <stdio.h>

int main() {
    int T, N, Ni;
    scanf("%d", &T);
    for (int i = 0; i < T; i++) {
        scanf("%d", &N);
        int arr[N];
        for (int j = 0; j < N; j++) {
            scanf("%d", &Ni);
            arr[j] = Ni;
        }
    }
    
}